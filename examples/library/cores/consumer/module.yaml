id:
  library: demo
  name: consumer
  vendor: example.com
  version: '0.1'
interfaces:
  stream_in:
    mode: subordinate
    signals:
      in:
        data: {bound: ['3', '0'], name: c_data}
        valid: {name: c_valid}
    type:
      library: demo
      name: demo_stream
      vendor: example.com
      version: '0.1'
signals:
  out:
    - {bound: ['3', '0'], name: out_1}
